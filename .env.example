# UE 3D Viewer – shared env (copy to .env). Used by scripts and Backend_API / Frontend_React.
# All values are optional; defaults are in Backend_API/src/config.js and Frontend_React/src/config.js.
# See Docs/ENV.md for full reference.

# -----------------------------------------------------------------------------
# Storage (Backend_API)
# -----------------------------------------------------------------------------
# STORAGE_ROOT=/path/to/workspace/Storage
# STORAGE_SUBDIR_RAW=raw
# STORAGE_SUBDIR_PROCESSED=processed
# STORAGE_SUBDIR_ANNOTATIONS=annotations
# STORAGE_SUBDIR_CONFIG=config

# -----------------------------------------------------------------------------
# Backend_API
# -----------------------------------------------------------------------------
PORT=3001
# CORS_ORIGIN=http://localhost:5173,http://localhost:5174
# PROJECTS_FILENAME=projects.json
# SHARING_FILENAME=sharing.json
# INTEGRATIONS_FILENAME=integrations.json
# UPLOAD_MAX_BYTES=524288000
# PRESENCE_STALE_MS=20000
# CACHE_MAX_AGE_LEVELS=10
# CACHE_MAX_AGE_CONFIG=10
# WORKER_MAX_CONCURRENT=2
# PYTHON_PATH=python3
# SCRIPTS_DIR= (optional; default Backend_API/scripts)
# SCRIPT_NAME_MESH=preprocess_meshes.py
# SCRIPT_NAME_POINTCLOUD=preprocess_pointclouds.py
# SCRIPT_NAME_GAUSSIAN=generate_gaussian_splats.py
# LOD_DECIMATE_RATIO=0.2
# LOD_POINTCLOUD_RATIO=0.2
# DEFAULT_USER_ID=default

# -----------------------------------------------------------------------------
# Frontend_React (Vite embeds VITE_* at build/dev)
# -----------------------------------------------------------------------------
VITE_API_URL=http://localhost:3001
# Pixel stream player URL: use 80 for UE embedded server (run-dev.sh); run-all.sh uses 1025
VITE_PIXEL_STREAM_URL=http://localhost:80
# Remote access: set both to the server IP so other devices can use the app, e.g.:
# VITE_API_URL=http://192.168.1.100:3001
# VITE_PIXEL_STREAM_URL=http://192.168.1.100:80
# Then run ./scripts/run-all.sh (frontend uses dev:host and shows Network URL).
# VITE_AUTH_STORAGE_KEY=ue3d_user
# VITE_DEFAULT_USER_ID=default
# VITE_PRESENCE_POLL_MS_VISIBLE=5000
# VITE_PRESENCE_POLL_MS_HIDDEN=10000
# VITE_PROCESSING_POLL_MS=2000

# -----------------------------------------------------------------------------
# Scripts (print-streamer-commands.sh, push_asset.py, run-ue-editor.sh, run-ue-streamer.sh)
# -----------------------------------------------------------------------------
# STREAMER_IP=127.0.0.1
# STREAMER_PORT=8888
# PLAYER_PORT=80
# RESOLUTION_X=1920
# RESOLUTION_Y=1080
# API_BASE_URL=http://localhost:3001
# Unreal Engine (GPU / Vulkan):
# UE_ROOT=/home/nvn/Rk_WorkSapace/UnrealEngine
# UE_EDITOR_BIN=/path/to/UnrealEditor   (default: $UE_ROOT/Engine/Binaries/Linux/UnrealEditor)
# UE_LINUX_NO_EDITOR=/path/to/UE_3DViewer_Project/LinuxNoEditor   (for run-ue-streamer.sh)

# -----------------------------------------------------------------------------
# SignallingWebServer: use its own config/CLI; set VITE_PIXEL_STREAM_URL to match.
# -----------------------------------------------------------------------------
