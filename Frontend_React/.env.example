# When using scripts from workspace root (run-all.sh, run-dev.sh), env is set from root .env.
# For local Frontend_React-only runs, copy from root .env.example or set here.
# Full list: see workspace root .env.example and Docs/ENV.md.

# Backend API base URL (for upload, projects, processing)
VITE_API_URL=http://localhost:3001

# Pixel Streaming player URL: where SignallingWebServer serves the player
VITE_PIXEL_STREAM_URL=http://localhost:80

# Optional (defaults in src/config.js): VITE_AUTH_STORAGE_KEY, VITE_DEFAULT_USER_ID,
# VITE_PRESENCE_POLL_MS_VISIBLE, VITE_PRESENCE_POLL_MS_HIDDEN, VITE_PROCESSING_POLL_MS
